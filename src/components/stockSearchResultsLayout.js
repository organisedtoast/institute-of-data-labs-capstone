// This shared layout object is the one page-width contract for search results.
// Any page that renders StockSearchResults should inherit the same width rules.
export const stockSearchResultsShellSx = {
  width: '100%',
  maxWidth: 1200,
  mx: 'auto',
  alignSelf: 'stretch',
};
